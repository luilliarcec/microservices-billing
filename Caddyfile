{
    frankenphp
    order php_server before file_server
    auto_https off # Disable for production
}

:8080 {
    root * /app/public
    encode gzip
    php_server
    file_server

    log {
            output stdout
            format console
        }
}
